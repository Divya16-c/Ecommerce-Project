import {useState} from 'react';import API from '../services/api';export default function AddProduct(){const[f,s]=useState({name:'',price:'',quantity:''});const c=e=>s({...f,[e.target.name]:e.target.value});const save=async e=>{e.preventDefault();await API.post('/products',f);alert('Saved');};return(<form onSubmit={save}><h3>Add Product</h3><input name='name' placeholder='Name' onChange={c}/><input name='price' placeholder='Price' onChange={c}/><input name='quantity' placeholder='Quantity' onChange={c}/><button>Save</button></form>);}